version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo doing the build here!
      - stackstate=$(aws cloudformation describe-stacks --stack-name wow-api-stack)
      - echo $stackstate
      - |
          if [ "X" = "X" ]; then
            echo was true--
          else
            echo was false--
          fi
      - aws cloudformation create-stack --stack-name wow-api-stack --template-body file://seededjob1.yaml
