version: 0.2

phases:
  install:
    runtime-versions:
        nodejs: 10
  build:
    commands:
      - echo doing the build here!

      - echo "hii"
      - |
        if expr "hi" : ".*build/MyProjectDev-" >/dev/null; then
          #yarn run build-dev;
        fi
      - |
        if expr "hii" : ".*build/MyProject-" >/dev/null; then
          #yarn run build-prod;
        fi
      - aws cloudformation create-stack --stack-name wow-api-stack --template-body file://seededjob1.yaml
